---
phase: 05-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/validators/__init__.py
  - src/validators/validator.py
  - tests/test_validator.py
autonomous: true
requirements:
  - VALID-01

must_haves:
  truths:
    - "Valid CanonicalTransaction passes validation with zero errors"
    - "Missing required field (id) produces ValidationError"
    - "Invalid decimal amount produces ValidationError"
    - "Negative amount produces ValidationError"
    - "Invalid transDate produces ValidationError"
    - "Invalid status value produces ValidationError"
    - "Multiple validation errors collected (not fail-fast)"
  artifacts:
    - path: "src/validators/__init__.py"
      provides: "Package exports (Validator, ValidationResult)"
      exports: ["Validator", "ValidationResult"]
    - path: "src/validators/validator.py"
      provides: "Validator class with required field, decimal, date, status validation"
      exports: ["Validator", "ValidationResult"]
    - path: "tests/test_validator.py"
      provides: "Comprehensive test suite for core validation rules"
      min_lines: 80
  key_links:
    - from: "src/validators/validator.py"
      to: "src/core/types.py"
      via: "imports CanonicalTransaction, ValidationError, TransactionStatus"
      pattern: "from src.core.types import.*CanonicalTransaction.*ValidationError"
    - from: "src/validators/validator.py"
      to: "src/core/enums.py"
      via: "imports TransactionStatus for status validation"
      pattern: "from src.core.enums import TransactionStatus"

---

<objective>
Build the core Validator service that validates CanonicalTransaction objects against business rules: required field checks, decimal validation (valid + non-negative), date validation, and status validation. Collects all errors (not fail-fast) and returns a ValidationResult.

Purpose: Gate between normalization (Phase 04) and persistence (Phase 06) — only valid transactions proceed to storage.
Output: Validator class, ValidationResult dataclass, comprehensive test suite
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase artifacts — types and models the Validator consumes
@src/core/types.py
@src/core/enums.py
@src/normalizer/normalizer.py

# Established test patterns
@tests/test_normalizer.py
</context>

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/core/types.py:
```python
class CanonicalTransaction(BaseModel):
    id: str
    trace: Optional[str] = None
    amount: Decimal
    currency: str
    status: TransactionStatus
    transDate: Optional[datetime] = None
    extra: dict[str, Any] = {}

class ValidationError(BaseModel):
    field: str
    reason: str
    row: Optional[int] = None
    trace: Optional[str] = None
```

From src/core/enums.py:
```python
class TransactionStatus(StrEnum):
    SUCCESS = "SUCCESS"
    FAILED = "FAILED"
    PENDING = "PENDING"
    REVERSED = "REVERSED"
```

From src/normalizer/normalizer.py (pattern to follow):
```python
# Error collection pattern used throughout codebase:
# - Methods return tuple[value | None, ValidationError | None]
# - Errors accumulated in list, never fail-fast
# - row_number passed for error context
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>task 1: Create Validator package with core validation skeleton and required field validation</name>
  <files>src/validators/__init__.py, src/validators/validator.py, tests/test_validator.py</files>
  <behavior>
    - Valid CanonicalTransaction (all fields present and correct) → ValidationResult with is_valid=True, errors=[]
    - CanonicalTransaction with empty/missing id → ValidationError(field="id", reason="required field 'id' is empty or missing")
    - CanonicalTransaction with valid id but missing amount → ValidationError(field="amount", reason="required field 'amount' is empty or missing")
    - Multiple missing fields → multiple ValidationErrors collected (not fail-fast)
    - row_number and trace passed through to ValidationError context
  </behavior>
  <action>
    Create the validators package structure:

    1. src/validators/__init__.py — Export Validator and ValidationResult
    2. src/validators/validator.py — Define:
       - ValidationResult dataclass: is_valid (bool), errors (list[ValidationError])
       - Validator class with validate() method accepting:
         - txn: CanonicalTransaction
         - row_number: Optional[int] = None
         - trace: Optional[str] = None
       - _validate_required_fields() method: checks id is non-empty string, amount is present, currency is non-empty, status is present
       - Required field validation per VALID-01: id (partner transaction ID) is mandatory — empty string or None produces ValidationError
    3. tests/test_validator.py — Test class TestRequiredFieldValidation with tests for:
       - Valid transaction passes (is_valid=True, no errors)
       - Missing id produces error
       - Empty id produces error
       - Multiple missing fields produce multiple errors
       - row_number propagated to errors
       - trace propagated to errors

    Follow the established error collection pattern from TransactionNormalizer: accumulate all errors, never fail-fast.
    Use TDD: write failing tests first, then implement.
  </action>
  <verify>
    <automated>python -m pytest tests/test_validator.py::TestRequiredFieldValidation -x -v</automated>
  </verify>
  <done>Required field validation works: valid transactions pass, missing/empty required fields produce ValidationError with correct field, reason, row, trace</done>
</task>

<task type="auto" tdd="true">
  <name>task 2: Add decimal validation (valid Decimal, non-negative) and date validation</name>
  <files>src/validators/validator.py, tests/test_validator.py</files>
  <behavior>
    - Valid positive Decimal amount → no error
    - Zero amount → no error (zero is valid)
    - Negative amount → ValidationError(field="amount", reason="amount must be non-negative")
    - Valid datetime transDate → no error
    - Note: transDate is Optional[datetime] — if None, no error (it's optional per CanonicalTransaction schema)
    - Note: amount type is already enforced by CanonicalTransaction.pydantic (float rejected) — validator checks business rule (non-negative)
  </behavior>
  <action>
    Add to Validator class:

    1. _validate_decimal() method:
       - Check amount >= 0 (non-negative business rule)
       - Negative amounts produce ValidationError(field="amount", reason="amount must be non-negative, got {amount}")
       - Zero is valid (refunds/zero-value transactions are legitimate)
       - Type correctness already enforced by CanonicalTransaction pydantic model — validator focuses on business rules

    2. _validate_date() method:
       - transDate is Optional[datetime] — if None, skip (it's optional)
       - If present, verify it's a datetime instance (type safety check)
       - Invalid type produces ValidationError(field="transDate", reason="transDate must be a datetime object")
       - Note: date parsing already done in TransactionNormalizer._convert_date — validator confirms type integrity

    3. Update validate() to call _validate_decimal() and _validate_date() after _validate_required_fields()

    4. Add tests in test_validator.py:
       - TestDecimalValidation class: positive amount passes, zero passes, negative fails
       - TestDateValidation class: None transDate passes (optional), valid datetime passes, invalid type fails
  </action>
  <verify>
    <automated>python -m pytest tests/test_validator.py::TestDecimalValidation tests/test_validator.py::TestDateValidation -x -v</automated>
</verify>
  <done>Decimal validation rejects negative amounts (zero allowed), date validation confirms transDate type integrity, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>task 3: Add status validation and comprehensive integration tests</name>
  <files>src/validators/validator.py, tests/test_validator.py</files>
  <behavior>
    - Valid TransactionStatus enum value → no error
    - Note: CanonicalTransaction already enforces status as TransactionStatus enum via pydantic — validator confirms it's one of the valid values
    - Full integration: valid transaction with all fields correct → is_valid=True
    - Full integration: transaction with multiple errors (missing id + negative amount) → multiple errors collected
  </behavior>
  <action>
    Add to Validator class:

    1. _validate_status() method:
       - Confirm status is a valid TransactionStatus enum member
       - Since CanonicalTransaction already coerces to TransactionStatus enum, this is a defensive check
       - Use: if txn.status not in TransactionStatus → ValidationError
       - In practice this should never fail after CanonicalTransaction construction, but validates the contract

    2. Update validate() to call _validate_status() in the validation chain

    3. Add tests in test_validator.py:
       - TestStatusValidation class: all four valid statuses pass (SUCCESS, FAILED, PENDING, REVERSED)
       - TestFullValidation class: integration tests combining all validation rules
         - Fully valid transaction → is_valid=True, 0 errors
         - Transaction with multiple issues → all errors collected
         - Error count matches number of violations
  </action>
  <verify>
    <automated>python -m pytest tests/test_validator.py -x -v</automated>
  </verify>
  <done>All core validation rules implemented and tested: required fields, decimal (non-negative), date (type integrity), status (enum membership), full integration with error collection</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Normalizer → Validator | Normalized data crosses here — must be validated before persistence |
| Validator → Persistence | Only validated transactions cross here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | Validator._validate_decimal | mitigate | Reject negative amounts explicitly — prevents injection of fraudulent credit transactions |
| T-05-02 | Tampering | Validator._validate_required_fields | mitigate | Require non-empty id field — prevents anonymous/untraceable transactions from entering system |
| T-05-03 | Repudiation | ValidationResult | mitigate | Collect ALL errors (not fail-fast) — complete audit trail of every validation failure per row |
| T-05-04 | Tampering | Validator._validate_status | mitigate | Validate status against TransactionStatus enum whitelist — prevents injection of arbitrary status values |
</threat_model>

<verification>
- All tests pass: `python -m pytest tests/test_validator.py -x -v`
- Validator class importable: `from src.validators import Validator, ValidationResult`
- ValidationResult has is_valid bool and errors list
- validate() method accepts CanonicalTransaction + optional row_number and trace
- No tests require MongoDB connection (pure unit tests)
</verification>

<success_criteria>
- src/validators/__init__.py exports Validator and ValidationResult
- src/validators/validator.py implements Validator with validate(), _validate_required_fields(), _validate_decimal(), _validate_date(), _validate_status()
- tests/test_validator.py has 15+ tests covering all validation rules
- All tests pass without MongoDB connection
- ValidationResult correctly reports is_valid and collected errors
- Multiple errors collected for transactions with multiple violations
</success_criteria>

<output>
After completion, create `.planning/phases/05-validation/05-01-SUMMARY.md`
</output>
